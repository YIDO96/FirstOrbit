# 주차별 상세 로드맵 (3주 압축판) — 물리 구현 & 학습 가이드

> 실질 개발 가능 기간: **약 3주**
> 목표 직군: 게임 클라이언트 프로그래머 (→ 향후 엔진)
> 방향: 물리 순도 우선. "직접 구현 + 개념 이해 + 설명 가능".

**스코프 원칙 (개정):** **우주선 발사까지(1~2주차)는 잘라내지 않고 전부 구현한다.**
카메라와 물리는 이 프로젝트의 자랑거리이므로 여기서 아끼면 남는 게 없다.
스코프 방어는 **3주차(궤도 게임화·폴리싱)에만** 적용한다. 아래 ✂️ 표시가 잘라낸 것들.

> ⚠️ **일정 리스크:** 1~2주차를 꽉 채우면 3주차가 압박받는다.
> 2주차 끝에 진도를 점검해서, 밀렸으면 3주차 항목을 우선순위대로 자른다.
> **자를 곳은 3주차이지 1~2주차가 아니다.**

**학습 로그 원칙:** 매주 "📚 공부 & 정리"를 `docs/study/`에 메모로 남긴다.
이 메모가 3주차 **기술 회고(포트폴리오 핵심)**의 재료가 된다.

> 📘 **주차별 학습 가이드** — 개념 · 수식 유도 · 왜 그런가 (주차 시작 전 / 막혔을 때)
> - `docs/study/1주차_학습가이드.md` ✅
> - `docs/study/2주차_학습가이드.md` (2주차 진입 시 작성)
> - `docs/study/3주차_학습가이드.md` (3주차 진입 시 작성)
>
> 🗓 **주차별 실행 가이드라인** — Day별 계획 · 시작 파라미터 · 소요시간 · 체크리스트
> (**매일 열어두는 문서**)
> - `docs/study/1주차_실행가이드라인.md` ✅
> - 2·3주차는 해당 주차 진입 시 작성
>
> 📚 **`docs/study/참고자료.md`** — 책·무료 자료 + **TODO ID별 "막히면 여기" 역인덱스**
> 주차 시작 전 20분 훑기용, 회고 인용 출처용.

---

## 1주차 — 스케일을 넘나드는 행렬 카메라 (기획의 심장 ①)

물리보다 **선형대수**가 주인공. 모든 렌더링의 기반. 여기 못 줄인다.

### 🔧 구현 작업
0. **배선 작업 (먼저 해야 나머지가 돌아간다)**
   - `SpaceWorld` 생성 → `.vcxproj` 등록 + `GameInstance::RegisterWorld()`에 등록
   - `Camera`는 지금 **아무도 안 쓰는 고아 클래스** → `World`가 멤버로 소유하고 `Enter()`에서 `Init()`
   - `InputManager`에 **마우스 휠 입력 추가** (`WM_MOUSEWHEEL` — 현재 없음)
   - `Vector2`에 `Normalized()` 추가 + 연산자 `const`화
1. **`Matrix3x3` 구현** — 곱셈 / `TransformPoint` (동차좌표) / `Rotate`
   - **아핀 전용 역행렬** (실제 사용) + **일반 3x3 역행렬** (여인수 전개, 검산·학습용)
   - 두 결과가 일치하는지 확인 → "왜 아핀 전용이면 충분한가"를 스스로 증명
2. `Camera` — 위치·줌·**회전** + 뷰 행렬 **`T(half)·S(zoom)·R(θ)·T(-pos)`** 조합
   + 역행렬로 `ScreenToWorld`
3. **표면 정렬 회전** (기획서 7.1 연출의 실체) — `θ = -π/2 - atan2(u.y, u.x)`
   - 행성 어느 지점에서 발사해도 **지평선이 수평**으로 보이게
   - 고도가 올라가면 정렬이 서서히 풀리며 탑뷰로 → **고도 하나가 줌·회전을 동시 구동**
   - `Q`/`E` 수동 회전 (검증 + 회고 GIF 촬영용)
4. **카메라 모드 2종** (기획서 5.1) — 고정 ↔ 우주선 중심, `Tab`/클릭 토글
   - ⚠️ 2단계로 간다: ① 성분별 행렬 보간을 **일부러 만들어 깨지는 걸 관찰**(📸)
     → ② 파라미터(위치·각도·줌) 개별 보간 후 재조립으로 교체
5. 마우스 휠 줌 / 드래그 팬 / 클릭 피킹(역행렬)
6. 행성 배치 — 물리 없이 **kinematic 원운동** (`x=cx+r·cosθ, y=cy+r·sinθ`)
7. 태양 + 행성 몇 개가 도는 화면 렌더 (스프라이트 아님 — **GDI 도형**으로 직접)
8. **거리별 LOD** — 화면상 반지름에 따라 표현 단계를 바꾼다
   - `r ≥ 30px`: 원 + 표면 디테일(지평선·명암) → `r ≥ 3px`: 단색 원 → `r < 3px`: 점 하나
   - 기획서 5.1의 "가까우면 행성 표면, 멀면 점 하나"를 그대로 구현
9. **변환 합성** — 우주선 도형·별 배경을 `View · T(pos) · R(angle)` 로 그린다
   - 카메라 회전이 공짜로 따라온다 (화면상 각도를 손으로 더할 필요 없음)

### 📚 공부 & 정리할 개념
- **2D 동차좌표**: 왜 (x,y,1)인가 → 이동을 행렬 곱으로 표현하려면 3x3 필요
  - 점 `(x,y,1)` vs 방향 `(x,y,0)` — 이동을 받느냐 안 받느냐
- **변환 행렬 SRT**: Scale·Rotation·Translation 각 행렬이 왜 그렇게 생겼는지
- **행렬 곱 순서**: 열벡터 규약에서 **오른쪽이 먼저 적용**된다
  - ⭐ **언제 교환이 되는가**: `S·R = R·S` (균등 스케일일 때만!)
  - 회전은 항상 원점 중심 → 돌릴 중심을 먼저 원점으로 옮겨야 한다
- **좌표 공간**: World / Screen / Window 3단 (마우스는 거꾸로 두 번 변환)
- **역행렬**: 아핀 블록 구조 `-A⁻¹t` — 왜 그냥 `-t`가 아닌가
- **로그 스케일 보간**: 줌을 지수/로그로 다뤄야 부드러운 이유
- **`atan2`**: 방향에서 각도 뽑기, 왜 `atan`이 아닌가 (사분면 보존)
- **각도 보간과 최단경로**: `350°→10°`가 왜 `-340°`로 도는가
  - ⚠️ `Util.cpp`의 `DegreeToRadian`은 `[0,2π)`로 정규화 → 차이 계산에 쓰면 안 됨
- **왜 행렬을 그냥 섞으면 안 되는가**: 회전 성분은 단위원 위의 점,
  직선으로 이으면 원 안쪽 = 스케일 축소. 180°면 영행렬

> 📝 정리: "이동이 왜 행렬 곱인가(동차좌표)" 한 장 + "행렬 보간이 왜 깨지는가" 한 장.
> 📘 상세 학습 자료: `docs/study/1주차_학습가이드.md`

### ✅ 체크포인트
휠로 지표면↔태양계 줌, 드래그 팬, 클릭 피킹, **표면 정렬(지평선 수평)**,
고도에 따른 정렬 해제, 카메라 모드 전환(파라미터 보간), LOD, 행성 원운동.
**(✂️ 없음 — 1주차는 전부 구현한다)**

> 📸 **회고 자료 촬영 필수 2컷:** ① 곱셈 순서를 틀렸을 때 화면이 좌상단으로 빨려드는 장면
> ② 성분별 행렬 보간으로 180° 전환 시 화면이 사라지는 장면.
> **고치기 전에 먼저 찍을 것.** 나중에 재현하려면 코드를 되돌려야 한다.

> 📌 **검산 습관:** `T(half)·S(s)·T(-c)`를 손으로 전개하면 `(world-c)*s + half`가 나온다.
> 행렬 코드의 결과가 이 식과 일치하는지 확인하면 단위 테스트가 된다.
> 곱셈 순서를 일부러 바꿔보고 화면이 어떻게 깨지는지 관찰할 것 — **교환법칙이 성립하지
> 않는다는 걸 버그로 체험하는 것**이 1주차 최고의 학습이다.

---

## 2주차 — 물리 코어 + 발사 (기획의 심장 ②, 가장 중요)

이 프로젝트의 **물리 실력 증명 구간**. 무슨 일이 있어도 사수.

### 🔧 구현 작업
1. **물리 바디**: 위치·속도·가속도 (vec2)
   - ⚠️ 위치는 반드시 `SetCenterPos()/GetCenterPos()`로 다룬다.
     `AActor`의 `_pos`는 **좌상단**이고 `_center`가 중심 — 물리는 전부 중심 기준이다.
2. **뉴턴 2법칙**: `a = F / m`
3. **중력**: `F = G·m·M / r²` — ⚠️ **우주선만** 받음 (행성끼리 X)
4. **추력**: 입력으로 진행 방향에 힘 추가
5. **적분기 3종** — 런타임 토글로 갈아끼운다 (⭐ 이 프로젝트 최고의 학습·면접 자료)
   ```
   ① 명시적 오일러      x += v*dt;  v += a*dt;      // 위치 먼저 (에너지 샘 → 발산)
   ② 반암시적 오일러     v += a*dt;  x += v*dt;      // 속도 먼저 (에너지 보존 → 안정)  ← 기본
   ③ RK4                k1~k4 가중평균                // 정확도 4차
   ```
   - 같은 초기값에서 셋을 나란히 돌려 **궤도가 어떻게 갈라지는지** 비교 자료로 남긴다
6. **발사 시퀀스 (전체 구현)**
   - 발사대 대기 → `발사 버튼` → **카운트다운 3·2·1** (`TIME.AddTimer`) → 이륙
   - `▲`/`W` 추력, `◀`/`▶`(`A`/`D`) 기체 기울이기 → 중력 턴
   - `R` 재시작 (상태 초기화)
   - 엔진음·발사음 (`SoundManager` 이미 구현됨)
7. **지표면 충돌 판정** — 중심 거리 < 행성 반지름이면 **발사 실패**
   - 궤도 판정(3주차)과 별개로, 발사 구간에서 실패가 성립해야 루프가 닫힌다
8. **대기권 연출 (정교하게)** — 고도 **하나의 값**이 세 가지를 동시에 구동
   - 하늘색: 파랑 → 어두운 파랑 → 검정 (다단계 보간)
   - 별: 고도에 따라 서서히 페이드인 (알파 대신 색 밝기로)
   - 카메라 줌: 고도에 비례해 자동 축소 (1주차 줌 보간 재사용)
9. **연료 관리** — 추력 사용량만큼 소모, HUD에 게이지
   - 기획서 4.2의 "**언제 추력을 떼는가**"를 실제 자원 압박으로 만든다
   - 무한 추력이면 타이밍 판단이 무의미해지므로 발사 구간의 핵심 규칙

### 📚 공부 & 정리할 개념 (⭐이 프로젝트의 핵심 학습)
- **벡터 연산**: 위치·속도·가속도, 정규화(단위벡터), 힘 합성
- **뉴턴 운동 법칙**: F=ma, 여러 중력원 → 합력
- **만유인력**: `F=G·m·M/r²`, 거리 제곱 반비례
- **수치 적분 (최중요)**:
  - 명시적 오일러 vs **반암시적 오일러** vs **RK4**
  - 왜 명시적은 궤도가 **발산**하고 반암시적은 유지되는가 (심플렉틱 = 에너지 보존 성질)
  - RK4는 왜 더 정확한데도 **궤도 게임엔 반암시적이 나은가** (장기 에너지 드리프트 vs 국소 정확도)
- **델타타임(dt)**: 프레임 독립 물리, dt 크면 왜 불안정한가

> 📝 정리: 같은 초기값으로 **적분기 3종 궤도 비교 GIF/스크린샷**.
> → 면접에서 바로 먹히는 자료. 이거 하나는 꼭 남길 것.

### ✅ 체크포인트
발사 버튼 → 카운트다운 → 이륙 → 대기권 돌파(하늘이 검게) → 중력 턴 조종 →
연료 소진 전 추력 컷오프. 지표면 재충돌 시 실패 → `R` 재시작.
**(✂️ 없음 — 2주차는 전부 구현한다)**

---

## 3주차 — 궤도 진입 게임화 + 마무리 + 기술 회고

물리를 "플레이"로 닫고, **포트폴리오로 포장**하는 주. 할 게 많으니 우선순위 순서대로.

### 🔧 구현 작업 (우선순위 높은 순)
1. **궤도 요소 계산 + 승리 조건** (최우선)
   - 현재 위치·속도로 근지점/원지점 추정
   - **근지점이 지표면보다 높으면 안정 궤도 = 클리어**
2. **최소 HUD**: 속도, 고도, 근지점 (목표를 이해시키는 최소 정보)
3. **궤도 예측선** (저비용·고효과 — 시간 되면 강력 추천)
   - 현재 상태를 **복사**해 앞으로 N스텝 미리 적분 → 점선 렌더
   - ⚠️ 액터를 통째로 값 복사하지 말 것. `AActor`는 컴포넌트 포인터를 소유하고
     소멸자에서 `delete`하므로 **더블 프리**가 난다. `{위치, 속도}` 구조체만 복사한다.
4. **카메라 자동 추적**: 우주선 따라가며 고도로 줌 보간
5. **기술 회고 문서 작성** (⭐포트폴리오 핵심 산출물, 반드시)

### 📚 공부 & 정리할 개념
- **궤도 요소**: 근지점/원지점/이심률의 의미
- **각운동량 보존**: 근지점에서 빠르고 원지점에서 느린 이유
- **역학적 에너지 보존**: 운동E + 위치E 일정
- **예측 시뮬레이션**: 상태 복제해 "미래를 적분"
- **충돌 감지 기초**: 원-원(중심 거리 vs 반지름 합)
  - 📌 엔진의 격자 브로드페이즈(`World`의 480x800 고정 격자)는 우주 좌표에서 무의미하다.
    콜라이더를 안 붙이고 `OrbitalGameMode`에서 "우주선 vs 홈 행성" 거리만 직접 비교한다.
    **"왜 범용 브로드페이즈를 안 썼는가"** 는 회고에 쓰기 좋은 판단 근거다.

> 📝 최종 산출물 (README/회고 문서):
> ① 적분기를 반암시적 오일러로 고른 이유 (+발산 비교 GIF)
> ② 스케일 부동소수점 문제를 카메라 로그 줌으로 푼 과정
> ③ 궤도 예측선 원리 (넣었다면)
> ④ 플레이 GIF/스크린샷
> **이 문서가 프로젝트 가치를 2배로 만든다.**

### ✅ 체크포인트
발사 → 궤도 진입 클리어까지 플레이 가능 + 기술 회고 문서 완성.

---

## ✂️ 잘라낸 것들 (스코프 방어 — **3주차 이후에만 적용**)

발사까지(1~2주차)는 아무것도 자르지 않는다. 아래는 **궤도 이후 영역**의 항목들이다.

| 잘라낸 것 | 이유 | 대체 |
|---|---|---|
| 착륙 판정(부드러운 착지) | 궤도 진입이 종료 지점 | 충돌 = 실패로 단순화 |
| vis-viva·케플러 심화 | 승리 조건엔 불필요 | 개념만 얕게 |
| 우주 자유비행/유랑 | 게임 하나를 더 만드는 규모 | 배경으로만 표현 |
| 달 전이·착륙·귀환 | 스코프 폭발 | 확장 아이디어로 기록만 |
| **n-body** (행성끼리 중력) | **물리 붕괴 위험 — 스코프가 아니라 안정성 문제** | 행성은 kinematic 고정, **중력은 우주선만** 받음 |
| 3D · 다중 우주선 · 네트워크 | 프로젝트 성격과 무관 | — |

> ⚠️ **n-body만은 "시간이 남아도" 넣지 않는다.** 잘라낸 이유가 일정이 아니라
> 시뮬레이션 안정성이기 때문이다 (기획서 5.3). 나머지와 성격이 다르다.

---

## 🛟 만약 밀리면 — **3주차에서만 자른다**

1~2주차를 꽉 채우는 대신, 3주차가 밀리면 **아래 역순으로** 잘라낸다.

| 자르는 순서 | 항목 | 잘랐을 때 |
|---|---|---|
| 1순위로 자름 | 궤도 예측선 (3-C) | 난이도만 올라감. 기능은 성립 |
| 2순위 | 이심률 기반 난이도 3단계 | "보통" 하나로 고정 |
| 3순위 | HUD 폴리싱 | 텍스트 나열로 충분 |
| **못 자름** | 궤도 요소 계산 · 승리 판정 · **기술 회고 문서** | 이게 없으면 프로젝트가 안 닫힌다 |

최악의 경우 승리 조건을 **"한 바퀴 안 떨어지고 공전하면 승리"** 로 단순화해도 성립한다.

> 📌 **발사까지 완성되면 그 자체로 이미 보여줄 게 있다.** 궤도 진입이 미완이어도
> "행렬 카메라 + 물리 발사 + 적분기 3종 비교"는 포트폴리오로 충분히 선다.
> 이게 1~2주차를 안 자르기로 한 진짜 이유다.

---

## 물리/수학 학습 지도 (한눈에)

| 주차 | 핵심 수학·물리 | 사수 여부 |
|---|---|---|
| 1주 | 선형대수(행렬·동차좌표·좌표변환) | 🔒 사수 |
| 2주 | 벡터·뉴턴역학·만유인력·**수치적분** | 🔒 사수 (최우선) |
| 3주 | 궤도역학·보존법칙·충돌 + **회고 문서** | 우선순위대로 |
