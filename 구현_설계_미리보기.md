# 구현 설계 미리보기 — 실제 코드 구조 기반

> 기획서 + 3주 로드맵을 **실제 코드에 대조해서** 매핑한 "예상 구현" 문서.
> 뼈대·데이터 흐름은 확정, `🎯 TODO`는 직접 구현할 핵심 수학/물리 로직.
> 클래스 관계도는 `설계_클래스다이어그램.md` 참고.

---

## 0. 기존 구조에 어떻게 얹히나

이 프로젝트는 언리얼식 `World / AActor / GameMode / Component` 구조가 이미 갖춰져 있다.
새 콘텐츠는 **기존 베이스 클래스를 상속**해서 얹는다.

### 0.1 손댈 파일

| 구분 | 파일 | 할 일 |
|---|---|---|
| ✏️ 수정 | `GameFramework/Camera.h/.cpp` | 줌 배율 + 로그 보간 + `ScreenToWorld` 추가 (현재 평행이동만) |
| ✏️ 수정 | `GameFramework/World.h/.cpp` | `Camera _camera;` 멤버 + `GetCamera()` 추가, `Enter()`에서 `Init` 호출 |
| ✏️ 수정 | `pch.h` 의 `Vector2` | `Normalized()` 추가 + **연산자 const화** |
| ➕ 신규 | `pch.h` 에 `Matrix3x3` | 2D 동차좌표 3x3 행렬 (Vector2 바로 아래에 둔다) |
| ✏️ 수정 | `Core/InputManager.h/.cpp` | **마우스 휠 델타** 지원 추가 (현재 없음) |
| ✏️ 수정 | `Core/GameInstance.cpp` | `RegisterWorld()`에 `SpaceWorld` 등록 |
| ✏️ 수정 | `FirstOrbit.vcxproj` | 신규 `.h/.cpp` 전부 등록 (VS에서 "기존 항목 추가"로 하면 자동) |
| ➕ 신규 | `Worlds/SpaceWorld.h/.cpp` | `World` 상속 — 우주 씬 |
| ➕ 신규 | `Actors/APlanet.h/.cpp` | `AActor` 상속 — kinematic 공전 + 중력원 |
| ➕ 신규 | `Actors/ASpaceship.h/.cpp` | `AActor` 상속 — 물리 바디 + 추력 |
| ➕ 신규 | `GameFramework/OrbitalGameMode.h/.cpp` | `GameMode` 상속 — 궤도 승리 판정 + HUD |

> 📌 **Camera는 현재 "고아 클래스"다.** 선언만 있고 어디서도 생성/`Init()`되지 않는다.
> 소유자를 `World`로 정하고(모든 씬이 하나씩 갖는다), `World::Enter()`에서 `Init` 한다.

### 0.2 ⚠️ 렌더링이 카메라를 거치지 않는다 (가장 중요한 사실)

현재 렌더 경로는 이렇다:

```
World::Render(hdc) → AActor::Render(hdc) → USpriteComponent::Render(hdc)
                   → Texture::RenderRotated(hdc, _owner->GetCenterPos(), radian)
                                              └─ 월드 좌표를 그대로 화면 좌표로 사용
```

즉 **카메라 변환이 들어갈 자리가 없다.** 게다가 `Texture::Render()`에는 배율 인자가 없어
(BitBlt 계열) **스프라이트를 줌에 맞춰 확대/축소할 수 없다.**

**결정: 우주 액터는 스프라이트 컴포넌트를 쓰지 않는다.**
`APlanet` / `ASpaceship`이 `Render()`를 직접 오버라이드해서, 카메라로 좌표를 변환한 뒤
GDI 도형(`Ellipse`, `LineTo`, `SetPixel`)으로 그린다.

**근거**
- 우주 게임의 그림은 원·선·점이 전부 → GDI 프리미티브로 충분하고 줌에 자연스럽게 대응
- `Texture`에 StretchBlt 경로를 새로 뚫는 것은 3주 스코프에서 순수 비용
- 기존 씬(Title/Main/GameOver/Editor)의 렌더 경로를 **전혀 건드리지 않음** → 회귀 위험 0

### 0.3 ⚠️ 충돌 브로드페이즈는 쓰지 않는다

`World`의 공간 분할 격자는 화면 크기(480x800)를 80으로 나눈 **고정 격자**다
(`World.h`의 `GridCellSize/GridCols/GridRows`). 좌표가 수십만 단위로 뻗는 우주에서는 의미가 없다.

**결정: 우주 액터는 `UColliderComponent`를 붙이지 않는다.**
`_collider == nullptr`이면 `CheckOverlaps()`가 알아서 건너뛰므로(`World.cpp`의 `continue`) 안전하다.
충돌 판정은 딱 하나 — "우주선 vs 홈 행성"뿐이고, `OrbitalGameMode`에서 **원-원 거리 비교**로 직접 한다.

```cpp
// 중심 거리 < 행성 반지름  =>  추락(실패)
if ((ship->GetCenterPos() - home->GetCenterPos()).Length() < home->GetBodyRadius())
    _state = EGameState::GameOver;
```

---

## 1주차 — 카메라 확장 + 우주 씬

### ✏️ Vector2 정리 (물리 계산의 토대)

`pch.h`의 `Vector2`에는 `Length()`, `LengthSquared()`, `Dot()`, `Cross()`, `Rotate()`,
`Normalize()`가 **이미 있다.** 물리 코드를 쓰기 전에 두 가지만 손본다.

```cpp
// 1) 자기 자신을 바꾸지 않고 단위벡터 "사본"을 돌려주는 버전 (중력 방향 계산에 필수)
Vector2 Normalized() const;   // 🎯 TODO(0-A)

// 2) 연산자/조회 함수들을 const로 (현재 전부 non-const라 const Vector2&를 못 받는다)
Vector2 operator+(const Vector2& o) const;   // 나머지 -, *, / 도 동일
float   Length() const;                      // LengthSquared, Dot, Cross, Rotate 도 동일
```

> 📚 **왜 이게 중요한가:** 기존 `Normalize()`는 **자기 자신을 바꾼다(in-place).**
> `Vector2 dir = planetPos - shipPos; dir.Normalize();` 는 되지만,
> `(planetPos - shipPos).Normalize()` 로는 결과를 못 받는다. 중력 방향처럼
> "원본은 두고 방향만 필요한" 계산이 계속 나오므로 `Normalized()`가 따로 있어야 한다.
> 이 둘의 차이(mutate vs return copy)는 물리 코드 버그의 단골 원인이다.

### ➕ Matrix3x3 (2D 동차좌표 행렬) — 기획의 심장

기획서 학습 목표 1번이 "**행렬** 기반 좌표 변환으로 카메라를 직접 구현"이다.
카메라 변환을 식으로 풀어 쓰는 대신, **진짜 3x3 행렬 자료구조를 만들어서** 조합한다.

```cpp
// pch.h — Vector2 바로 아래
// 열벡터 규약: p' = M * p,  p = (x, y, 1)ᵀ.  m[행][열].
struct Matrix3x3
{
    float m[3][3] = {};

    static Matrix3x3 Identity();
    static Matrix3x3 Translate(Vector2 t);   // 이동
    static Matrix3x3 Scale(float s);         // 균등 스케일 (줌)
    static Matrix3x3 Rotate(float radian);   // 회전 (지금은 안 쓰지만 확장 여지)

    Matrix3x3 operator*(const Matrix3x3& o) const;  // 🎯 TODO(0-B)
    Vector2   TransformPoint(Vector2 p) const;      // 🎯 TODO(0-C)  (x,y,1) — 이동 받음
    Vector2   TransformVector(Vector2 v) const;     //     (x,y,0) — 이동 무시(방향용)
    Matrix3x3 Inverse() const;                      // 🎯 TODO(0-D)  아핀 전용 (실사용)
    Matrix3x3 InverseGeneral() const;               // 🎯 TODO(0-D') 일반 3x3 (검산용)

    // ⚠️ 성분별 보간 — 회전이 섞이면 깨진다. 실사용 X, "왜 안 되는지" 관찰 전용.
    static Matrix3x3 Lerp(const Matrix3x3& a, const Matrix3x3& b, float t);  // 🎯 TODO(0-E')
};
```

```cpp
Matrix3x3 Matrix3x3::operator*(const Matrix3x3& o) const
{
    // 🎯 TODO(0-B): 표준 행렬 곱
    //   result.m[i][j] = Σ_k  m[i][k] * o.m[k][j]
    //   ⚠️ 결과를 자기 자신에 누적하지 말 것 (읽는 중인 값을 덮어쓴다)
}

Vector2 Matrix3x3::TransformPoint(Vector2 p) const
{
    // 🎯 TODO(0-C): (x, y, 1)을 곱한 뒤 앞 두 성분만 취한다
    //   x' = m[0][0]*p.x + m[0][1]*p.y + m[0][2]*1
    //   y' = m[1][0]*p.x + m[1][1]*p.y + m[1][2]*1
    //   → 세 번째 열이 "이동"으로 작동하는 이유를 여기서 눈으로 확인할 것
}

Matrix3x3 Matrix3x3::Inverse() const
{
    // 🎯 TODO(0-D): 아핀 변환 전용 역행렬 (마지막 행이 항상 0 0 1인 성질을 이용) — 실제 사용
    //
    //   M = | A  t |      =>   M⁻¹ = | A⁻¹  -A⁻¹t |
    //       | 0  1 |                 |  0      1  |
    //
    //   A는 좌상단 2x2.  2x2 역행렬은 A⁻¹ = (1/det) * | d  -b |
    //                                                  | -c  a |
    //   det = a*d - b*c.  det이 0에 가까우면(줌 0) 항등행렬을 돌려주고 빠져나온다.
}

Matrix3x3 Matrix3x3::InverseGeneral() const
{
    // 🎯 TODO(0-D'): 일반 3x3 역행렬 — 여인수(cofactor) 전개  ※ 학습·검산 전용
    //
    //   det = m00(m11·m22 - m12·m21) - m01(m10·m22 - m12·m20) + m02(m10·m21 - m11·m20)
    //   M⁻¹ = (1/det) · adj(M),   adj(M) = 여인수 행렬의 "전치"   ← 전치를 빼먹기 쉽다
    //
    //   검산: 뷰 행렬에 대해 Inverse()와 InverseGeneral() 결과가 일치해야 한다.
    //         일치를 확인하고 나면 "아핀 전용으로 충분한 이유"를 스스로 증명한 셈이다.
}
```

> 📚 **왜 역행렬을 두 개 만드는가:** 실제로 쓰는 건 아핀 버전 하나다. 일반 버전은
> **"내가 짠 지름길이 진짜 역행렬과 같은가"를 검증하는 장치**다. 게임 코드에서 최적화된
> 경로를 만들 때 항상 필요한 습관 — 느리지만 확실한 참조 구현을 옆에 두고 대조한다.
> 확인이 끝나면 `InverseGeneral()`은 지우지 말고 남겨서 회고 자료로 쓴다.

> 📚 **학습 연결(⭐):** 여기가 1주차의 진짜 학습 지점이다.
> - `TransformPoint`에서 **왜 (x,y,1)인가** — 3번째 성분 1이 `m[i][2]`(이동)를 살려낸다.
>   `TransformVector`는 (x,y,0)이라 이동이 곱해지지 않는다. **점과 방향의 차이**가 이 1과 0이다.
> - `Inverse`의 블록 구조 `-A⁻¹t` — "먼저 회전/스케일을 되돌리고, 그 좌표계에서 이동을 뺀다".
>   순서가 뒤바뀌면 안 되는 이유가 식에 그대로 박혀 있다.

### ✏️ Camera 확장 (핵심)

```cpp
class Camera
{
public:
    void  Init(int width, int height);
    void  Update(float dt);                     // ➕ 줌 보간 후 뷰 행렬 재계산

    void  SetPosition(Vector2 pos) { _position = pos; }
    Vector2 GetPosition() const    { return _position; }

    void  SetZoom(float z)         { _targetZoom = z; }         // 목표 줌 (보간됨)
    void  SetZoomImmediate(float z){ _zoom = _targetZoom = z; }  // 씬 진입 시 초기값
    void  AddZoom(float delta)     { _targetZoom *= (1.f + delta); } // 휠 입력용
    float GetZoom() const          { return _zoom; }

    // ➕ 회전 (기획서 7.1 — 지표면에서 지평선이 수평으로 보이게)
    void  SetRotation(float radian){ _targetRotation = radian; }
    void  AddManualRotation(float d){ _manualRotation += d; }    // Q/E 디버그용
    float GetRotation() const      { return _rotation; }

    // 변환은 전부 행렬을 거친다
    Vector2 WorldToScreen(Vector2 worldPos)  const { return _view.TransformPoint(worldPos); }
    Vector2 ScreenToWorld(Vector2 screenPos) const { return _invView.TransformPoint(screenPos); }
    float   WorldToScreenScale(float worldLen) const { return worldLen * _zoom; }

    const Matrix3x3& GetViewMatrix() const { return _view; }

    // ➕ 카메라 모드 2종 (기획서 5.1) — Tab 또는 우주선 클릭으로 토글
    void SetMode(ECameraMode mode);              // 전환 시작 (즉시 안 바뀜, 보간됨)
    void SetFollowTarget(AActor* target) { _target = target; }
    ECameraMode GetMode() const { return _mode; }

private:
    void UpdateViewMatrix();     // 🎯 TODO(1-A)
    void BlendParams(float dt);  // 🎯 TODO(0-E)  파라미터 보간 (실사용)

    Vector2 _position;           // 카메라의 World상 중심 좌표
    float   _zoom       = 1.f;   // 1=등배, 작을수록 축소(멀리서 봄)
    float   _targetZoom = 1.f;   // 보간 목표

    float   _rotation       = 0.f;  // 현재 회전각 (라디안)
    float   _targetRotation = 0.f;  // 고도가 구동하는 목표 회전각
    float   _manualRotation = 0.f;  // Q/E 디버그 오버라이드 (최종 = target + manual)

    Matrix3x3 _view;             // 월드 → 스크린
    Matrix3x3 _invView;          // 스크린 → 월드 (역행렬 피킹)

    // 모드 전환 보간 상태 — ⚠️ 행렬이 아니라 "파라미터"를 스냅샷한다
    ECameraMode _mode     = ECameraMode::Fixed;
    AActor*     _target   = nullptr;   // 참조만 (소유 X)
    float       _blend    = 1.f;       // 0=이전 모드, 1=현재 모드
    Vector2     _blendFromPos;
    float       _blendFromRot  = 0.f;
    float       _blendFromZoom = 1.f;

    int _width = 0, _height = 0;
};

enum class ECameraMode
{
    Fixed,      // 행성 기준 고정 — 우주선이 화면을 가로질러 공전
    Follow,     // 우주선 중심 — 뷰 행렬이 우주선을 따라간다
};
```

```cpp
// 🎯 TODO(0-E') [1단계 — 실패 관찰용] Matrix3x3::Lerp, 성분별 선형 보간
//   result.m[i][j] = a.m[i][j] * (1-t) + b.m[i][j] * t
//
//   두 모드의 회전각 차이를 크게(90°~180°) 두고 전환시켜 볼 것:
//     90°  차이 → 화면이 약 30% 쪼그라들었다 돌아온다 (스케일 0.707)
//     180° 차이 → 중간값이 영행렬이 되어 화면이 한 점으로 사라진다
//   📸 반드시 스크린샷/GIF를 남길 것. 회고 문서의 핵심 자료다.
//
//   왜: 회전 성분 (cosθ, sinθ)는 단위원 위의 점이다. 두 점을 직선으로 이으면
//       원 안쪽을 지나므로 길이가 1보다 작아진다 = 스케일이 줄어든다.

// 🎯 TODO(0-E) [2단계 — 실제 사용] Camera::BlendParams
//   행렬을 섞지 말고 "재료"를 각각 보간한 뒤 매 프레임 재조립한다.
//     위치  : 선형 보간
//     각도  : 최단경로 보간 (delta를 ±π로 접는다)   ← 아래 ⚠️
//     줌    : 로그 보간 (TODO 1-B에서 만든 것 재사용)
//   그다음 UpdateViewMatrix()로 T·S·R·T 재조립 → _invView도 다시 만든다.
//
//   SetMode에서 현재 파라미터 3개를 _blendFrom*에 스냅샷하고 _blend = 0 으로.
//   ⚠️ 전환 도중 다시 토글하면 "현재 보간된 값"을 새 스냅샷으로 삼아야 안 튄다.
//
//   ⚠️ 각도 보간에 Util.cpp의 DegreeToRadian/RadianToDegree를 끼워넣지 말 것.
//      두 함수 모두 각도를 [0,2π)/[0,360)으로 정규화해서(Util.cpp:9, :20)
//      음수 delta를 양수로 바꿔버린다 → 350°→10° 가 +20°가 아니라 -340°가 된다.
```

> 📚 **각 파라미터가 사는 공간이 다르다는 게 핵심이다.**
> 위치는 덧셈 공간, 줌은 곱셈 공간, 각도는 원 위. **셋을 뭉뚱그려 행렬 성분으로
> 섞으면 세 공간의 구조가 전부 무시된다.** 그래서 깨진다.
> (자세한 설명·유도는 `docs/study/1주차_학습가이드.md` A-10)

> 📌 현재 `WorldToScreen`은 `_width/_height`를 저장해놓고 정작 전역 `GWinSizeX/Y`를 쓴다.
> 멤버 `_width/_height`로 통일할 것 — 그래야 `Init`이 의미를 가진다.
> 📌 `_view`/`_invView`는 **매 프레임 한 번만** 다시 만든다. 액터마다 다시 만들면
> 행렬을 쓰는 이득(합성 결과 재사용)이 사라진다. 이게 행렬 자료구조의 존재 이유다.

```cpp
void Camera::UpdateViewMatrix()
{
    Vector2 half = Vector2(_width / 2.f, _height / 2.f);

    // 🎯 TODO(1-A): 네 변환을 올바른 "순서"로 곱해 뷰 행렬을 만든다
    //   의미: ① 카메라를 원점으로 ② 회전 ③ 줌 ④ 화면 중심으로
    //   _view    = Matrix3x3::Translate(half)
    //            * Matrix3x3::Scale(_zoom)
    //            * Matrix3x3::Rotate(_rotation + _manualRotation)
    //            * Matrix3x3::Translate(-_position);
    //   _invView = _view.Inverse();       // 🎯 TODO(1-A') 역행렬 피킹은 이 한 줄로 끝난다
    //
    //   ⚠️ 곱셈 순서를 바꿔보면 학습이 끝난다.
    //      · Scale을 맨 앞으로  → 화면 중심 이동까지 줌에 곱해져 좌상단으로 빨려든다
    //      · Rotate를 T(-pos) 앞으로 → 월드 원점 기준으로 돌아 카메라가 멀수록 크게 휩쓸린다
    //        (회전은 항상 원점 중심 — 돌리고 싶은 중심을 먼저 원점으로 옮겨야 한다)
    //
    //   📌 검증은 2단계로: 먼저 _rotation=0으로 두고 `(p-pos)*zoom+half` 와 대조,
    //      통과한 뒤에 회전을 켜고 `zoom*Rotate(p-pos,θ)+half` 와 대조한다.
}

void Camera::Update(float dt)
{
    // 🎯 TODO(1-B): _zoom을 _targetZoom으로 "로그 스케일"로 부드럽게 보간
    //   힌트: 선형 lerp가 아니라 비율 기반(지수) 보간이 스케일에 자연스럽다
    //   _zoom *= pow(_targetZoom / _zoom, 1 - exp(-k*dt))  같은 형태

    UpdateViewMatrix();    // 보간된 줌을 행렬에 반영
}
```

> 📚 학습 연결: 검산해보면 `T(half)·S(s)·R(θ)·T(-c)` 를 전개한 결과가
> `screen = s·Rotate(world - c, θ) + half` 와 정확히 같다 (θ=0이면 `(world-c)*s + half`).
> **행렬 코드가 맞게 돌아가는지 이 식으로 단위 테스트하듯 확인**할 수 있다.

### ➕ 표면 정렬 회전 — 기획서 7.1 연출의 실체

```cpp
// 🎯 TODO(1-F): 지표면에서 "지평선이 수평"으로 보이게 만드는 각도
//
//   ① 로컬 "위" = 행성 중심에서 바깥으로
//        Vector2 u = (기준점 - 행성중심).Normalized();
//   ② 그 방향의 현재 각도
//        float phi = atan2f(u.y, u.x);        // ⚠️ atan 말고 atan2 (사분면 보존)
//   ③ 화면 위쪽 (0,-1)의 각도는 -π/2. R(θ)는 φ를 φ+θ로 보내므로:
//        theta = -PI/2 - phi;
//
//   ④ 고도에 따라 정렬을 풀어준다 (t는 줌·하늘색·별이 이미 쓰는 그 값)
//        float t = clamp(altitude / atmosphereHeight, 0, 1);
//        _targetRotation = LerpAngle(theta, 0.f, EaseInOut(t));
//
//   ⚠️ 검증: 발사대를 행성의 위/아래/좌/우 네 지점에 놓고 매번 지평선이 수평인지.
//      한 지점만 보면 부호를 틀려도(+PI/2-phi, phi-PI/2 등) 우연히 맞아 보인다.
```

> 📚 **고도 하나가 `줌 · 하늘색 · 별 밝기 · 회전` 네 가지를 동시에 구동한다.**
> 기획서 7.1의 *"별도 화면 교체가 아니라 하나의 뷰 행렬이 줌으로 연속 변화하는 것"* 이
> 코드에서는 이 `t` 하나로 떨어진다. 2주차 대기권 연출(`2-F`)이 같은 `t`를 공유한다.

### ➕ 변환 합성 — 회전하는 카메라 아래서 그리기

```cpp
// 🎯 TODO(1-G): 로컬 → 월드 → 스크린 2단 합성
//
//   [우주선] 로컬 좌표의 도형 점들을 한 번에 화면으로 보낸다
//     Matrix3x3 M = cam.GetViewMatrix()
//                 * Matrix3x3::Translate(GetCenterPos())
//                 * Matrix3x3::Rotate(DegreeToRadian(GetDegree()));
//     for (로컬 점 p) : screenPts.push_back(M.TransformPoint(p));
//     Polygon(hdc, screenPts, n);
//
//   → 카메라 회전이 "공짜로" 따라온다. 화면상 각도를 손으로 더할 필요가 없다.
//
//   [별 배경] 화면 좌표에 고정으로 찍으면 안 된다 (월드만 돌아 몰입이 깨진다).
//     별을 월드 좌표 점으로 생성해두고 cam.WorldToScreen()을 통과시킨다.
```

> 📌 **설계 판단 정정:** 앞서 "액터 계층이 평평해서 변환 합성이 필요 없다"고 적었는데,
> 카메라 회전이 들어오면서 **예외가 생겼다.** 우주선 도형을 그리려면 로컬→월드→스크린
> 2단 합성이 실제로 필요하다. 행렬 클래스를 만든 판단이 그만큼 더 강해졌다.

### ✏️ World가 Camera를 소유

```cpp
// World.h
public:
    Camera& GetCamera() { return _camera; }
protected:
    Camera _camera;

// World.cpp — Enter() 맨 앞
_camera.Init(GWinSizeX, GWinSizeY);
// World.cpp — Update() 맨 앞
_camera.Update(deltaTime);
```

> 기존 4개 씬은 렌더에 카메라를 쓰지 않으므로 이 변경의 영향을 받지 않는다.

### ✏️ InputManager 휠 지원 (없어서 새로 만든다)

1주차 목표인 "마우스 휠 줌"에 필요한 입력이 현재 **없다.**

```cpp
// InputManager.h
float GetWheelDelta() { return _wheelDelta; }   // 이번 프레임 휠 이동량 (+위 / -아래)
private:
    float _wheelDelta = 0.f;

// Update() 맨 끝에서 0으로 리셋 (이벤트성 값이라 매 프레임 소비)
// WndProc의 WM_MOUSEWHEEL에서 GET_WHEEL_DELTA_WPARAM(wParam) / WHEEL_DELTA 를 넣어준다
```

> 📌 마우스 좌표를 게임 좌표로 쓸 때 주의: 화면은 **레터박스 스케일**되어 있다.
> `GameInstance::GetGameViewportRect()`로 실제 게임 화면 사각형을 얻어
> 창 좌표 → 게임 좌표(480x800)로 먼저 환산한 다음 `ScreenToWorld`에 넣어야 한다.

### ➕ SpaceWorld

```cpp
class SpaceWorld : public World
{
    using Super = World;
public:
    virtual void Enter() override;              // 태양/행성/우주선 스폰
    virtual void Update(float deltaTime) override;
    virtual void Render(HDC hdc) override;      // 배경(하늘색/별) → Super::Render

protected:
    virtual GameMode* CreateGameMode() override;   // ➕ OrbitalGameMode를 물려준다
    virtual void OnSceneGUI() override;
    // 텍스처를 안 쓰므로 전용 섹션을 만들거나, json에 없는 이름을 줘도 안전하다
    // (World::loadResources가 섹션을 못 찾으면 그냥 return)
    virtual wstring GetResourceSectionName() const override { return L"SpaceScene"; }

private:
    APlanet*    _home = nullptr;
    ASpaceship* _ship = nullptr;
};
```

> 📌 `CreateGameMode()` 오버라이드가 `OrbitalGameMode`를 붙이는 **유일한 지점**이다.
> `World::Enter()`가 `loadResources()` → `CreateGameMode()` → `Init(this)` 순으로 부르므로,
> `SpaceWorld::Enter()`는 **`Super::Enter()`를 먼저 호출한 뒤** 액터를 스폰해야
> GameMode가 액터를 찾을 수 있다. (또는 GameMode가 첫 `Update`에서 늦게 찾도록)

### ➕ APlanet (물리 없는 원운동)

```cpp
class APlanet : public AActor
{
    using Super = AActor;
public:
    virtual void Init() override;
    virtual void Update(float deltaTime) override;   // 🎯 TODO(1-C): 매개변수 원운동
    virtual void Render(HDC hdc) override;           // 카메라 거쳐 원으로 그리기

    void Setup(Vector2 orbitCenter, float orbitRadius, float orbitSpeed,
               float mass, float bodyRadius);

    float GetMass()       const { return _mass; }
    float GetBodyRadius() const { return _bodyRadius; }

private:
    Vector2 _orbitCenter;
    float _orbitRadius = 0, _orbitSpeed = 0, _angle = 0;
    float _mass = 0, _bodyRadius = 0;   // 중력·충돌은 2·3주차에 사용
};
```

```cpp
void APlanet::Update(float deltaTime)
{
    // 🎯 TODO(1-C): 매개변수 원운동
    //   _angle += _orbitSpeed * deltaTime;
    //   Vector2 p = _orbitCenter + Vector2(cosf(_angle), sinf(_angle)) * _orbitRadius;
    //   SetCenterPos(p);            // ⚠️ _pos에 직접 대입하지 말 것 (아래 주의 참고)

    Super::Update(deltaTime);        // 컴포넌트 갱신 + _center 재계산
}
```

> ⚠️ **좌표 규칙 주의 (틀리기 쉬운 지점)**
> `AActor`의 표준은 `_pos` = **좌상단**, `_center` = **중심**이고,
> `AActor::Update()`가 매 프레임 `_center = _pos + _size/2` 로 **덮어쓴다.**
> 궤도 물리는 전부 중심 기준이므로 반드시 `SetCenterPos()`로 위치를 넣고,
> 읽을 때도 `GetCenterPos()`를 쓴다. `_pos`에 직접 대입하면 `_size`만큼 어긋난다.
> (도형으로 그리는 액터는 `_size`를 `(bodyRadius*2, bodyRadius*2)`로 맞춰두면 깔끔하다)

### 렌더 흐름 (씬 → 카메라 → 화면)

```
SpaceWorld::Render(hdc)
  ├ 배경: 고도 기반 하늘색 FillRect + 별 점 찍기
  └ Super::Render(hdc) → 각 액터의 Render()
       APlanet::Render:
           Camera& cam = _ownerWorld->GetCamera();
           Vector2 s = cam.WorldToScreen(GetCenterPos());
           float   r = cam.WorldToScreenScale(_bodyRadius);
           // 🎯 TODO(1-E): 화면상 반지름 r로 표현 단계를 고른다
           //   r >= 30 : 원 + 표면 디테일 (지평선 곡률, 명암) — 지표면 클로즈업
           //   r >=  3 : 단색 원                              — 우주에서 본 행성
           //   r <   3 : 점 하나 (SetPixel)                   — 멀리 있는 천체
```

> 📚 **LOD 판정 기준이 "월드 반지름"이 아니라 "화면상 반지름"인 게 핵심이다.**
> 카메라 줌이 바뀌면 같은 행성이 다른 단계로 그려진다. 즉 **거리와 줌을 하나의 값으로
> 합쳐서 판단**하는 것이고, 그 값을 `WorldToScreenScale`이 이미 만들어준다.
> 기획서 5.5의 "스케일 부동소수점 붕괴" 대응도 같은 원리다 — 화면에서 1px도 안 되는
> 것을 정밀하게 계산할 이유가 없다.

✅ **1주차 끝:** 휠로 지표면↔우주 줌, 드래그 팬, 클릭 피킹, 행성 원운동.

---

## 2주차 — 물리 코어 + 발사 (심장)

### ➕ ASpaceship (물리 바디)

```cpp
class ASpaceship : public AActor
{
    using Super = AActor;
public:
    virtual void Init() override;
    virtual void Update(float deltaTime) override;   // 중력+추력→적분
    virtual void Render(HDC hdc) override;

    void    ApplyThrust(bool on)  { _thrusting = on; }
    Vector2 GetVelocity() const   { return _velocity; }
    float   GetMass()     const   { return _mass; }

    // 적분기 비교 GIF용 토글 (2주차 최고의 학습 자료)
    void        SetIntegrator(EIntegrator i) { _integrator = i; }
    EIntegrator GetIntegrator() const        { return _integrator; }

    // 연료
    float GetFuel()      const { return _fuel; }
    float GetFuelRatio() const { return _fuelMax > 0 ? _fuel / _fuelMax : 0.f; }
    bool  HasFuel()      const { return _fuel > 0.f; }

private:
    Vector2 AccelerationAt(Vector2 pos) const;   // 중력 가속도 (RK4가 중간 지점에서 호출)

    Vector2 _velocity;
    float _mass        = 1.f;
    float _thrustPower = 0.f;
    bool  _thrusting   = false;

    EIntegrator _integrator = EIntegrator::SemiImplicitEuler;

    float _fuel        = 0.f;    // 남은 연료
    float _fuelMax     = 0.f;
    float _fuelBurn    = 0.f;    // 초당 소모량
};

enum class EIntegrator
{
    ExplicitEuler,      // 위치 먼저 — 에너지가 새어나가 궤도 발산
    SemiImplicitEuler,  // 속도 먼저 — 에너지 대체로 보존 (기본값)
    RK4,                // 4차 룽게-쿠타 — 국소 정확도 최고
};
```

> 📌 **`AccelerationAt(pos)`를 따로 뺀 이유:** RK4는 "현재 위치"가 아니라
> **가상의 중간 위치 4곳에서 가속도를 물어본다.** 힘 계산이 `Update` 안에 인라인되어
> 있으면 RK4를 못 붙인다. 적분기를 갈아끼울 수 있게 만들려면 **"상태 → 가속도" 함수가
> 순수 함수로 분리**되어 있어야 한다. 이게 적분기 교체 가능한 설계의 핵심이다.

```cpp
void ASpaceship::Update(float deltaTime)
{
    Vector2 totalForce = Vector2();
    Vector2 myPos = GetCenterPos();

    // 1) 중력 합력 — 월드의 모든 행성을 순회
    for (int32 i = 0; i < _ownerWorld->GetActorCount(); ++i)
    {
        APlanet* planet = dynamic_cast<APlanet*>(_ownerWorld->GetActor(i));
        if (!planet) continue;

        // 🎯 TODO(2-B): F = G * m * M / r^2, 방향은 행성 쪽 단위벡터
        //   Vector2 diff = planet->GetCenterPos() - myPos;
        //   float r2 = diff.LengthSquared();            // sqrt 한 번 아끼는 습관
        //   totalForce += diff.Normalized() * (G * _mass * planet->GetMass() / r2);
        //   ⚠️ r2가 0에 가까울 때(행성 중심 근처) 힘이 폭발한다 — 하한을 두거나 표면에서 컷
    }

    // 2) 추력 (기체 정면 방향으로) — 연료가 있을 때만
    if (_thrusting && HasFuel())
    {
        totalForce += GetForwardDir() * _thrustPower;
        _fuel = max(0.f, _fuel - _fuelBurn * deltaTime);   // 🎯 TODO(2-E)
    }

    // 3) 적분
    Vector2 a = totalForce / _mass;
    // 🎯 TODO(2-C): 반암시적 오일러
    //   _velocity += a * deltaTime;               // 속도 먼저
    //   SetCenterPos(GetCenterPos() + _velocity * deltaTime);  // 갱신된 속도로 위치
    //
    //   비교용 명시적 오일러(EIntegrator::ExplicitEuler):
    //   SetCenterPos(GetCenterPos() + _velocity * deltaTime);  // 위치 먼저 (이전 속도로)
    //   _velocity += a * deltaTime;
    //
    // 🎯 TODO(2-D): RK4 — 중력만 대상으로 (추력은 입력이라 중간 지점에서 재평가하지 않는다)
    //   k1v = AccelerationAt(x)           ,  k1x = v
    //   k2v = AccelerationAt(x + k1x*h/2) ,  k2x = v + k1v*h/2
    //   k3v = AccelerationAt(x + k2x*h/2) ,  k3x = v + k2v*h/2
    //   k4v = AccelerationAt(x + k3x*h)   ,  k4x = v + k3v*h
    //   x += (k1x + 2*k2x + 2*k3x + k4x) * h/6
    //   v += (k1v + 2*k2v + 2*k3v + k4v) * h/6

    Super::Update(deltaTime);
}
```

> 📚 **학습 연결(⭐⭐) — RK4가 더 정확한데 왜 기본값이 아닌가**
> RK4는 **한 스텝의 오차**가 훨씬 작다. 그런데 궤도 게임에서 중요한 건 한 스텝이 아니라
> **수만 스텝 뒤에도 궤도가 안 무너지는가**다. 반암시적 오일러는 심플렉틱(symplectic)이라
> 에너지가 일정 범위 안에서 진동할 뿐 **한 방향으로 새지 않는다.** RK4는 정확하지만
> 에너지 보존이 보장되지 않아 장시간 돌리면 서서히 드리프트한다.
> → **"정확도"와 "장기 안정성"은 다른 축**이라는 것. 이 문장 하나가 회고 문서의 하이라이트다.
> 세 적분기를 나란히 돌린 GIF가 그 증거가 된다.

> ⚠️ **스코프 방어:** 행성끼리는 서로 안 당긴다. **중력을 받는 건 우주선(ASpaceship)뿐.**
> 📚 **학습 연결(⭐):** `_semiImplicit` 토글로 같은 초기값에서 궤도가
> 발산(명시적)하는지 유지(반암시적)되는지 GIF로 남긴다. → 이 프로젝트 최고의 면접 자료.
> 📌 `deltaTime`은 `GMaxDeltaTime`(1/10초)로 이미 상한이 걸려 있어(`pch.h`)
> 창 드래그·중단점으로 dt가 폭증해도 적분이 터지지 않는다. 이 안전장치도 회고에 쓸 재료다.

✅ **2주차 끝:** 지표면 발사 → 대기권 돌파 → 중력에 휘는 우주선을 추력으로 조종.

---

## 3주차 — 궤도 승리조건 + HUD + 회고

### ➕ OrbitalGameMode (승리 판정)

```cpp
class OrbitalGameMode : public GameMode
{
    using Super = GameMode;
public:
    virtual void Init(World* world) override;
    virtual void Update(float deltaTime) override;   // 궤도 판정
    virtual void OnGUI() override;                   // HUD

private:
    ASpaceship* _ship = nullptr;
    APlanet*    _home = nullptr;

    float _perigee = 0.f;   // 근지점
    float _apogee  = 0.f;   // 원지점
    float _altitude = 0.f;  // 현재 고도 (표면 기준)
};
```

```cpp
void OrbitalGameMode::Update(float deltaTime)
{
    Vector2 rVec = _ship->GetCenterPos() - _home->GetCenterPos();
    Vector2 vVec = _ship->GetVelocity();
    float mu = G * _home->GetMass();     // 표준 중력 파라미터

    // 🎯 TODO(3-A): 궤도 요소
    //   r = rVec.Length(), v = vVec.Length()
    //   비에너지    eps = v*v/2 - mu/r
    //   장반경      a   = -mu / (2*eps)
    //   각운동량    h   = rVec.Cross(vVec)        // 2D에선 스칼라
    //   이심률      e   = sqrt(1 + 2*eps*h*h / (mu*mu))
    //   _perigee = a*(1-e);  _apogee = a*(1+e);

    // 🎯 TODO(3-B): 승리 / 실패 판정
    //   실패: r < _home->GetBodyRadius()                  => 추락
    //   실패: eps >= 0                                    => 탈출 (쌍곡선 궤도)
    //   승리: 대기권 밖 && _perigee > 대기권 고도          => 안정 궤도 클리어
}
```

> 📚 학습 연결: 근지점 공식은 "각운동량 보존 + 에너지 보존"에서 유도된다.
> 유도 과정을 메모로 남기면 3주차 학습이 정리된다.
> `eps < 0`이면 타원(묶인 궤도), `eps >= 0`이면 탈출 — 이 부호 하나가 승패를 가른다.

### ➕ 궤도 예측선 (저비용·고효과, 시간 되면)

```cpp
// ⚠️ 액터를 값으로 복사하면 안 된다!
// AActor는 _components 포인터들을 소유하고 소멸자에서 delete하므로,
// ASpaceship을 값 복사하면 같은 포인터를 두 번 delete(더블 프리)한다.
// 예측에 필요한 건 위치와 속도뿐이므로 순수 상태 구조체만 복사한다.
struct OrbitState
{
    Vector2 pos;
    Vector2 vel;
};

vector<Vector2> OrbitalGameMode::PredictPath(int steps, float dt) const
{
    OrbitState ghost{ _ship->GetCenterPos(), _ship->GetVelocity() };
    vector<Vector2> path;
    path.reserve(steps);

    for (int i = 0; i < steps; ++i)
    {
        // 🎯 TODO(3-C): 추력 없이 중력만으로 dt씩 전진 (본체와 같은 적분기 사용)
        //   ghost.vel += gravity(ghost.pos) / mass * dt;
        //   ghost.pos += ghost.vel * dt;
        //   path.push_back(ghost.pos);
    }
    return path;   // Render에서 Camera.WorldToScreen 거쳐 점선으로
}
```

> 📚 학습 연결: **예측이 본체와 같은 적분기를 써야** 화면의 점선과 실제 궤적이 일치한다.
> 다르면 "예측선대로 안 간다"는 버그로 보인다 — 시뮬레이션 일관성의 좋은 예.

### HUD (ImGui 이미 통합됨 → OnGUI에서)

`World::OnGUI()`가 `_gameMode->OnGUI()`를 불러주므로 그냥 채우면 된다.

```
단계 | 속도 | 고도 | 근지점(perigee) | 원지점(apogee)
목표: 근지점을 대기권 위로!
```

✅ **3주차 끝:** 발사→궤도 진입 클리어 플레이 가능 + 기술 회고 문서.

---

## 전체 데이터 흐름 (한 프레임)

```
GameInstance::Update()
  └ WorldManager → SpaceWorld::Update(dt)
       ├ Camera.Update(dt)                  // 줌 보간 (World::Update 선두)
       ├ 입력 처리 (휠 줌 / 드래그 팬 / 클릭 피킹 / 추력·기울임)
       ├ for actor: actor->Update(dt)
       │     APlanet    → 원운동(1-C)
       │     ASpaceship → 중력합력(2-B) + 추력 + 적분(2-C)
       ├ CheckOverlaps()                    // 콜라이더 없음 → 사실상 no-op
       └ GameMode->Update(dt)               // OrbitalGameMode: 궤도 판정(3-A,3-B)

GameInstance::Render()
  └ SpaceWorld::Render(hdc)
       ├ 배경 (고도 기반 하늘색 + 별)
       ├ for actor: Camera.WorldToScreen로 투영 후 GDI 도형
       └ 예측선 점선 (3-C)

GameInstance::OnGui()
  └ SpaceWorld::OnGUI() → OnSceneGUI() + GameMode::OnGUI() (HUD)
```

---

## 🎯 직접 구현할 핵심 로직 목록 (학습 포인트)

| ID | 위치 | 무엇을 | 배우는 것 |
|---|---|---|---|
| 0-A | `Vector2::Normalized` | 단위벡터 사본 반환 | mutate vs return copy |
| 0-B | `Matrix3x3::operator*` | 행렬 곱 | 행렬 곱의 정의 |
| 0-C | `Matrix3x3::TransformPoint` | 동차좌표 적용 | **왜 (x,y,1)인가** |
| 0-D | `Matrix3x3::Inverse` | 아핀 역행렬 | 블록 구조 `-A⁻¹t` |
| 0-D' | `Matrix3x3::InverseGeneral` | 일반 3x3 역행렬 (검산) | 여인수·행렬식 |
| 0-E' | `Matrix3x3::Lerp` | 성분별 보간 (**실패 관찰용**) | 왜 깨지는가 (⭐) |
| 0-E | `Camera::BlendParams` | 파라미터 보간 (실사용) | 파라미터마다 다른 공간 |
| 1-A | `Camera::UpdateViewMatrix` | `T·S·R·T` 뷰 행렬 조합 | **곱셈 순서 (⭐)** |
| 1-A' | `Camera::ScreenToWorld` | 역행렬 피킹 | 역변환의 의미 |
| 1-B | `Camera::Update` | 로그 스케일 줌 보간 | 지수 보간 |
| 1-C | `APlanet::Update` | 매개변수 원운동 | 삼각함수·궤적 |
| 1-D | `Camera::SetMode` | 카메라 모드 전환 | 스냅샷·재진입 처리 |
| 1-E | `APlanet::Render` | 화면 반지름 기반 LOD | 거리+줌의 단일 척도 |
| 1-F | `Camera` | **표면 정렬 회전** | `atan2`·각도 유도 (⭐) |
| 1-G | `ASpaceship::Render`, 별 | 로컬→월드→스크린 합성 | 변환 합성 |
| 2-B | `ASpaceship::AccelerationAt` | 만유인력 합력 | 뉴턴·힘 합성 |
| 2-C | `ASpaceship::Update` | 명시적/반암시적 오일러 | 수치적분 (⭐) |
| 2-D | `ASpaceship::Update` | **RK4 적분기** | 정확도 vs 장기 안정성 (⭐⭐) |
| 2-E | `ASpaceship::Update` | 연료 소모 | 자원이 만드는 타이밍 압박 |
| 2-F | `SpaceWorld::RenderBackground` | 고도→하늘색·별·줌 | 단일 값으로 연출 몰기 |
| 3-A | `OrbitalGameMode` | 근지점/원지점 계산 | 궤도역학·보존법칙 |
| 3-B | `OrbitalGameMode` | 승리/실패 판정 | 상태 규칙 |
| 3-C | `PredictPath` | 미래 적분 예측선 | 예측 시뮬레이션 |

이 23개가 "직접 손으로 짜서 이해했다"를 증명하는 지점. 나머지 뼈대는 기존 구조 재활용.

> ⚠️ **1주차 15개 · 2주차 5개 · 3주차 3개** — 발사까지를 안 자르기로 해서 앞이 무겁다.
> 다만 `0-B`/`0-C`/`0-E'`는 정의를 그대로 옮기는 기계적 작업(각 10분)이고,
> 실제로 시간이 드는 건 **`0-D`, `1-A`, `0-E`, `1-F`** 넷이다.
> `1-G`(변환 합성)는 2주차 우주선 렌더에서 어차피 필요했던 작업을 앞당기는 것이다.

> 📌 **3대 학습 축이 각 주차에 하나씩 박혀 있다.**
> 1주차 `1-A`(행렬 곱 순서) · 2주차 `2-C`(적분기 선택) · 3주차 `3-A`(보존법칙).
> 회고 문서의 세 챕터가 그대로 이 셋이다.

---

## 부록 — 기존 코드에서 그냥 가져다 쓰는 것들

| 필요 | 이미 있는 것 |
|---|---|
| 델타타임 | `TIME.GetDT()` (`GMaxDeltaTime`으로 상한 걸림) |
| 입력 | `INPUT.GetButtonPressed/Down/Up(KeyType::…)`, `GetMousePos()` |
| 회전 | `AActor::SetRotation/AddRotation` → `_degree/_forwardDir/_rightDir` 자동 동기화 |
| 씬 전환 | `WORLD.ChangeWorld("SpaceWorld")` |
| 디버그 토글 | `GAME.IsDebugMode()` (F1) |
| HUD | ImGui 통합 완료 — `OnGUI()`에서 바로 사용 |
| 사운드 | `SoundManager` (엔진음·발사음) |
| 타이머 | `TIME.AddTimer(func, interval, loop)` — 카운트다운 3·2·1에 사용 |

> 📌 `KeyType`에 `Tab`, `R`이 없다 — 카메라 토글/재시작 키를 쓰려면 `InputManager.h`의
> `enum class KeyType`에 `Tab = VK_TAB`, `R = 'R'` 을 추가할 것.
